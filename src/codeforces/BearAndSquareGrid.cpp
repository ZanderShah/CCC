#include <bits/stdc++.h>
using namespace std;

int n, k, g[501][501];
queue<int> q;
string s;

int main()
{
	cin >> n >> k;
	for (int i = 1; i <= n; i++)
	{
		cin >> s;
		for (int j = 1; j <= n; j++)
			g[i][j] = (s[j - 1])
	}
}